{
  "name": "techishiring-twitter-bot-v2",
  "version": "2.0.0",
  "description": "The Twitter bot that powers the hashtag #TechIsHiring",
  "main": "bot.js",
  "scripts": {
    "start": "node dist/bot.js",
    "build": "npx tsc && cp config/config.env dist/config/config.env",
    "dev": "npm run build && npm start",
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "npm ci && npx tsc"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/TechIsHiring/techishiring-twitter-bot-v2"
  },
  "author": "Chad Stewart",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.0.3",
    "twitter-api-v2": "^1.12.8"
  },
  "devDependencies": {
    "@types/node": "^18.8.4",
    "typescript": "^4.8.4"
  }
}
